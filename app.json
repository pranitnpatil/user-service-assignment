{
  "name": "Start on Heroku: Python",
  "description": "django backed app with some basic custom APIs",
  "image": "heroku/python",
  "repository": "",
  "keywords": ["python", "django" ],
  "env": {
    "SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "test": "python manage.py test"
      }
    }
  }
}
